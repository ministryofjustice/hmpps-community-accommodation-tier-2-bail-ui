{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "../components/formFields/form-page-radios/macro.njk" import formPageRadios %}

{% extends "../partials/layout.njk" %}

{% block pageTitle %}
    What type of application are you applying for?
{% endblock %}

{% block beforeContent %}
  {{ govukBackLink({
    text: "Back",
    href: paths.applications.beforeYouStart()
  }) }}
{% endblock %}

{% block content %}

  <form action="{{ paths.applications.chooseApplicationType() }}" method="post">
    <input type="hidden" name="_csrf" value="{{ csrfToken }}"/>

    {{
      formPageRadios({
        fieldName: "applicationType",
        fieldset: {
          legend: {
            text: "You are applying for:",
            isPageHeading: true,
            classes: "govuk-fieldset__legend--l"
          }
        },
        items: [
          {
            value: "courtBail",
            text: "Court bail"
          },
          {
            value: "prisonBail",
            text: "Prison bail"
          }
        ]
      })
    }}

    {{ govukButton({ text: "Confirm" }) }}
  </form>
{% endblock %}
